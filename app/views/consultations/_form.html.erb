<%= simple_form_for([@patient, @patient.consultations.build]) do |f| %>

  <!--Content of the form-->
  <%= f.input :cat, label: "CAT" %>
  <%= f.input :x_ray, label: "X-RAY" %>
  <%= f.input :other, label: "Others" %>
  <%= f.button :submit %>

<% end %>
